import 'package:flutter/material.dart';

/// EMPLOYEE — SHARED LAYOUT
class EmployeeSpacing {
  static const pageHorizontal = 10.0;
  static const pageVertical = 40.0;

  static const boxConstraintsWidthMax = 1200.0;
  static const leftBoxSidebarWidth = 300.0;
}

/// EMPLOYEE — RADIUS
class EmployeeRadius {
  static const avatarRadius = 60.0;
  static const card = 20.0;

  // General card + buttons
  static const generalCard = 10.0;
  static const button = 5.0;

  // Sidebar / nav row
  static const sidebarStatusPill = 6.0;
  static const navRow = 3.0;
}

/// EMPLOYEE — TEXT SIZES
class EmployeeTextSize {
  static const normal = 15.0;

  // EmployeeView header
  static const headerName = 32.0;

  // Status pill
  static const statusPill = 15.0;
}

/// EMPLOYEE VIEW — SPACING
class EmployeeViewSpacing {
  static const headerToContentGap = 20.0;
  static const sidebarToContentGap = 16.0;

  // Sidebar spacing
  static const avatarRadius = 100.0;
  static const avatarToStatusGap = 15.0;
  static const statusToNavGap = 15.0;

  // Status pill padding
  static const statusPillPaddingHorizontal = 6.0;
  static const statusPillPaddingVertical = 1.0;

  // Nav list width
  static const navListWidth = 280.0;

  // Divider
  static const dividerHeight = 1.0;

  // Nav row padding
  static const navRowPaddingHorizontal = 20.0;
  static const navRowPaddingVertical = 20.0;

  // Nav row inner spacing
  static const navRowIconTextGap = 12.0;
}

/// EMPLOYEE GENERAL VIEW — SPACING
class EmployeeGeneralSpacing {
  // Outer padding (read view)
  static const scrollPaddingHorizontal = 20.0;
  static const scrollPaddingVertical = 1.0;

  // Layout constraint (read view)
  static const constrainedBoxMaxWidth = 1000.0;

  // Card padding (read view)
  static const cardPaddingHorizontal = 18.0;
  static const cardPaddingVertical = 10.0;

  // Key/Value rows
  static const keyValueRowVerticalPadding = 10.0;
  static const keyLabelWidth = 260.0;
  static const keyValueGap = 18.0;

  // bottom gap under list
  static const listBottomGap = 10.0;

  // Edit form padding
  static const editFormPaddingHorizontal = 20.0;
  static const editFormPaddingVertical = 5.0;

  // Form spacing
  static const fieldSpacing = 16.0;

  // Buttons spacing
  static const buttonSpacing = 12.0;

  // Edit button sizing (read view button)
  static const editButtonMinWidth = 8.0;
  static const editButtonHeight = 36.0;

  // if needed
  static const iconSize = 20.0;
}

/// EMPLOYEE — COLORS
class EmployeeColors {
  static final cardBorder = Colors.grey.shade300;
}

/// EMPLOYEE — ASSETS
class EmployeeAssets {
  static const profileImage = 'assets/images/profile.jpg';
}

