import 'package:flutter/material.dart';

class MainScaffoldIds {
  static const routeName = 'main_scaffold';
  static const routeKey = 'main_scaffold';
}

class ScaffoldColors {
  static const navTextColor = Colors.black;
}

class ScaffoldTextSize {
  static const navTitle = 30.0;
}

class ScaffoldSpacing {
  static const navGap = 20.0;

  // Settings overlay size
  static const settingsWidth = 400.0;
  static const settingsHeight = 300.0;
}

class ScaffoldIcons {
  static const settings = Icons.settings;
}
