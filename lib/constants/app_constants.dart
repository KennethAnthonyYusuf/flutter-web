import 'package:flutter/material.dart';

class AppColors {
  static const backgroundPrimary = Colors.white;
  static const backgroundSecondary = Color(0xFFEFF1F5);
  static const backgroundTertiary = Color(0xFF467DF2);
  static const backgroundQuaternary = Colors.black;
  static const backgroundQuinary = Color(0xFF007BFF);

  static const textPrimary = Colors.black;
  static const textSecondary = Colors.grey;
  static const textTertiary = Colors.blue;
  static const textQuaternary = Colors.white;

  static const transparent = Colors.transparent;
  static const black87 = Colors.black87;
}

class AppElevation {
  static const none = 0.0;
  static const small = 2.0;
  static const medium = 4.0;
  static const large = 8.0;
}

class AppDurations {
  static const navAnimation = Duration(milliseconds: 150);
}
